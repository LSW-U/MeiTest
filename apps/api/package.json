{
  "name": "@meimart/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'TODO: nest start --watch (D4-T1)' && exit 0",
    "build": "echo 'TODO: nest build' && exit 0",
    "typecheck": "tsc --noEmit",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:migrate:create": "prisma migrate dev --create-only",
    "db:seed": "echo 'TODO: tsx prisma/seed.ts (D2-T5)' && exit 0",
    "db:studio": "prisma studio",
    "db:reset": "prisma migrate reset",
    "clean": "rm -rf dist .turbo src/prisma/client"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
