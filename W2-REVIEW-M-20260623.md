# MeiMart W2 流程 M 审查报告

- **日期**：2026-06-23
- **审查者**：Hermes Agent（流程 M 审核员）
- **范围**：W2 流程 M（治理/财务）— `w2-flow-m` 分支
- **结论**：⚠️ 修复后可整合（1 个 P0 + 7 个 P1 需处理）

---

## 一、全局概览

| 流程 | 目录 | 分支 | commit 数 | typecheck | test | manifest |
|---|---|---|---|---|---|---|
| M | MeiMart-m | w2-flow-m | **0** | ✅ | ✅ | ❌ |

> **关键问题**：HEAD == w1-complete (7ae4c38)，所有改动均为未提交的工作区变更（19 个 modified + 21 个 untracked）。没有任何 W2 commit。

---

## 二、问题清单

### P0 — 阻断整合（必须修复）

| # | 问题 | 文件 | 修复建议 |
|---|---|---|---|
| P0-1 | **全部改动未提交** — HEAD 停在 w1-complete，19 个 modified 文件 + 21 个 untracked 文件全在工作区。无 commit 历史、无 manifest、无法 git merge。 | 整个工作区 | 按 task 粒度分 commit，格式 `[W2-M-C{n}-T{n}] 简述`，然后输出 W2-M-MANIFEST.md |

### P1 — 强烈建议修复

| # | 问题 | 文件 | 修复建议 |
|---|---|---|---|
| P1-1 | **无 W2-M-MANIFEST.md** — §4 要求每个流程输出 manifest 供主 AI 整合。缺失将导致整合成本高、易错。 | repo 根目录 | 按 §4.1 模板生成 manifest，列出所有独占文件 + 共享文件改动 |
| P1-2 | **gen:types 产物过期** — `pnpm --filter @meimart/shared-types gen:types` 后 `git diff` 有变更。`warehouseName` 类型不一致：工作区为 `string`，重新生成后为 `{ [key: string]: string }`（I18nText）。 | packages/shared-types/src/api-types.ts L826, L1525 | 重新跑 gen:types 并提交更新后的 api-types.ts |
| P1-3 | **文件边界违规：(warehouse) 路由组** — M 流程创建了 `apps/admin-web/src/app/(warehouse)/warehouse/page.tsx`，但 §2.1 明确将 `(warehouse)/**` 划归 W 流程独占。 | apps/admin-web/src/app/(warehouse)/warehouse/page.tsx | 删除此文件，或改为在 M 流程自己的路由组下创建占位页（如 `(platform)/warehouse-stub/`）。整合时由 W 流程填充 |
| P1-4 | **跨流程数据依赖无契约标记** — `dashboard.service.ts` 直接查询 `db.order`、`db.riderLocation`、`db.warehouse`（C/W 流程表），且硬编码订单状态字符串 `'CONFIRMED'`、`'PICKED'`、`'OUT_FOR_DELIVERY'` 等。这些状态值属于 C 流程的订单状态机，无 TODO/contract 标记。C 流程若改状态名，dashboard 会静默返回错误数据。 | apps/api/src/modules/platform/dashboard.service.ts L25-33, L130 | 加 `// TODO(C-flow-contract): 订单状态值依赖 C 流程 OrderStatus enum，整合时对齐` 注释；或从 shared-types 引入 OrderStatus 常量 |
| P1-5 | **dead export 抑制 unused warning** — `dashboard.controller.ts` 导入了 `Public` 装饰器但未使用，创建 `_platformDashboardPublic = Public` 哑导出规避告警。 | apps/api/src/modules/platform/dashboard.controller.ts L13, L33 | 删除 `import { Public }` 和 L33 的哑导出 |
| P1-6 | **Dashboard raw SQL 硬编码 UTC 时区** — 趋势查询用 `AT TIME ZONE 'UTC'`，但项目市场是东帝汶（Asia/Dili, UTC+9）。Dashboard 时间桶（如 "08:00"）将显示 UTC 时间，与用户本地时间差 9 小时。 | apps/api/src/modules/platform/dashboard.service.ts L118-119 | 改为 `AT TIME ZONE 'Asia/Dili'` 或从 SystemConfig 读取时区配置 |
| P1-7 | **Audit controller 错误码越段** — 无效 UUID 返回 `E-COMMON-001`，但 §3.4 将 `E-AUDIT-*` 段分配给 M 流程。审计相关错误应使用 `E-AUDIT-*`。 | apps/api/src/modules/platform/audit.controller.ts L56 | 改为 `E-AUDIT-002`（或新增 `E-AUDIT-002: Invalid audit log id`） |

### P2 — 可推迟到整合后

| # | 问题 | 文件 | 修复建议 |
|---|---|---|---|
| P2-1 | **settle / im 模块未创建** — W3 范围，可接受。Schema 已预建 Settlement/WithdrawalRequest model。 | — | W3 实现时创建 `apps/api/src/modules/settle/` 和 `apps/api/src/modules/im/` |
| P2-2 | **audit 模块放在 platform 内** — §2.3 列 `audit/**` 为独立目录，但任务分解文档说"audit(并入 platform)"。AI 遵循了任务分解。文档不一致。 | apps/api/src/modules/platform/audit.* | 整合时由主 AI 决策：保持现状（platform 子文件）或拆为独立 module |
| P2-3 | **CSV 导出非流式** — 10000 行全部在内存中拼接为字符串。 | apps/api/src/modules/platform/audit.service.ts L60-117 | 大数据量时改用 stream，MVP 阶段可推迟 |
| P2-4 | **dashboard SQL 冗余表达式** — `bucketExpr` 变量已计算，但 SELECT 子句内联了相同表达式而非使用变量。 | apps/api/src/modules/platform/dashboard.service.ts L116-124 | SELECT 子句也用 `${bucketExpr}` 引用变量 |
| P2-5 | **菜单动态渲染未实现** — C2 任务"菜单按视角动态渲染(读 state)"未做。只有 PerspectiveSwitcher 下拉框，无侧边栏菜单按视角过滤。 | apps/admin-web/src/components/ | W3/W4 补侧边栏菜单组件 |
| P2-6 | **其他视角占位页** — (merchant)/(support)/(rider-mgmt) 页面由 M 流程创建，不在 §2.3 M 流程前端独占范围内。但 C2 任务要求"各视角首页落地"。 | apps/admin-web/src/app/(merchant)/(support)/(rider-mgmt)/ | 整合时保留占位页，W/C 流程替换为真实内容 |
| P2-7 | **UUID 校验过于宽松** — `/^[0-9a-f-]{36}$/i` 允许非法 UUID（如全 hyphen）。 | apps/api/src/modules/platform/audit.controller.ts L54 | 改用 zod `z.string().uuid()` 或更严格的正则 |
| P2-8 | **Tetum 翻译为英文** — platform.json 和 errors.json 的 tet 版本均为英文。与 W1 决策一致（"Tetum 留接口空翻译"）。 | packages/shared-locales/tet/ | 后期补 Tetum 翻译 |

---

## 三、逐维度审查详情

### 维度 1：文件边界

**后端**：✅ 只创建了 `apps/api/src/modules/platform/**`，未碰 W/C 独占后端文件。
**W1 文件**：✅ 未碰 auth/health/me/shared/infrastructure。
**共享文件**：✅ 正确修改了 app.module.ts / schema.prisma / seed.ts / errors.json / shared-locales/index.ts / api-contract/index.ts / common.ts / gen-openapi.ts。
**前端**：❌ 创建了 `(warehouse)/` 占位页 — 违反 §2.1（W 流程独占）。其他占位页 (merchant)/(support)/(rider-mgmt) 不在任何流程的明确独占范围内，属于灰色地带。

### 维度 2：命名规范

- Prisma model：✅ `SystemConfig` / `Settlement` / `WithdrawalRequest` — PascalCase 业务名，无流程前缀
- Migration：✅ `20260623120000_add_platform_settle_m` — `_m` 后缀正确
- Schema export：✅ `DashboardSummary` / `AuditLogListItem` / `SystemConfigItem` — camelCase + 业务名
- 错误码：✅ `E-PLATFORM-001/002` / `E-AUDIT-001` — 在 §3.4 M 流程范围内
- i18n key：✅ `platform.json` 独占 namespace，未碰 common.json
- Seed 分段：✅ `// === FLOW M ===` 注释分段

### 维度 3：共享文件改动记录

| 共享文件 | 改动 | manifest 记录 |
|---|---|---|
| app.module.ts | + PlatformModule import + imports 数组 | ❌ 无 manifest |
| schema.prisma | + SystemConfig / Settlement / WithdrawalRequest (末尾追加) | ❌ 无 manifest |
| migrations/ | + 20260623120000_add_platform_settle_m/migration.sql | ❌ 无 manifest |
| api-contract/index.ts | + `export * from './platform'` | ❌ 无 manifest |
| api-contract/common.ts | ErrorResponse 正则加 SETTLE/IM/AUDIT | ❌ 无 manifest |
| gen-openapi.ts | + 7 个 platform path 注册 | ❌ 无 manifest |
| shared-locales/index.ts | + 5 语言 platform bundle | ❌ 无 manifest |
| errors.json ×5 | + E-PLATFORM-001/002 + E-AUDIT-001 | ❌ 无 manifest |
| seed.ts | + FLOW M 段（9 个 system configs） | ❌ 无 manifest |
| openapi.yaml | 重新生成（16 paths, 33 schemas） | ✅ 产物已更新 |
| api-types.ts | **过期** — warehouseName 类型不一致 | ❌ 需重新生成 |

### 维度 4：代码质量

- **typecheck**：✅ 9 个 workspace 全过
- **test**：✅ 134 tests passed（含新增 14 tests：platform-time 8 + system-config 6）
- **gen:openapi**：✅ 产物与工作区一致（16 paths, 33 schemas）
- **gen:types**：❌ 产物过期（warehouseName: string → 应为 object）

### 维度 5：跨流程依赖标记

**Dashboard 数据依赖（无 mock 标记）**：
- `db.order.groupBy()` — 依赖 C 流程 Order 表 + 硬编码状态字符串
- `db.riderLocation.count()` — 依赖 C 流程 RiderLocation 表
- `db.warehouse.findMany()` — 依赖 W 流程 Warehouse 表
- `db.order.count()` — 依赖 C 流程 Order 表

代码注释提到"payment 数据 W5 切真，当前用订单估算"，但无 `TODO` 或 `MOCK` 标记。W2 阶段表存在但无数据，查询返回零值，不会报错。但硬编码的订单状态字符串 `'CONFIRMED'/'PICKED'/'OUT_FOR_DELIVERY'` 等是隐式契约依赖，C 流程若改状态名会静默出错。

**SystemConfig**：无跨流程依赖 ✅
**Audit**：复用 W1 AuditLog 表，无跨流程依赖 ✅
**Perspective store**：M 流程自建 zustand store，W 流程的 perspective 基础架构（C2 task）由 M 流程实现。无 mock 需要 ✅

### 维度 6：安全检查

- **@Roles('super_admin')**：✅ 三个 controller 全部标注（DashboardController / AuditController / SystemConfigController）
- **APP_GUARD 全局鉴权**：✅ 依赖 W1 全局 JwtAuthGuard，无需手动 @UseGuards
- **硬编码密钥**：✅ 无
- **SQL 注入**：✅ raw SQL 用 Prisma tagged template（`$queryRaw\`...\``），参数自动参数化
- **Logger mask**：✅ 使用 W1 的 logger（ALS traceId + phone/idCard mask）
- **CORS / JWT secret**：✅ 未改动
- **@Audit 装饰器**：✅ SystemConfigController.update 标注了 @Audit，变更会自动写 AuditLog

### 维度 7：W1 已完成项重复检查

- ✅ 未重复 auth/RBAC/JWT/device_type
- ✅ 未重复 OTP 策略
- ✅ 未重复 PaymentStrategy
- ✅ 未重复 i18n 基础（只新增 platform namespace）
- ✅ 未重复 docker compose
- ✅ 未重复 RealtimeGateway
- ✅ 未重复 GIST 索引

---

## 四、整合风险预判

| 风险点 | 严重度 | 详情 | 整合时处理方案 |
|---|---|---|---|
| **(warehouse) 路由组冲突** | 高 | M 流程创建了 `(warehouse)/warehouse/page.tsx`，W 流程 §2.1 也独占 `(warehouse)/**`。整合时 git merge 会冲突。 | 删除 M 流程的 (warehouse) 占位页，由 W 流程创建 |
| **api-types.ts 不同步** | 中 | warehouseName 类型不一致（string vs object）。整合后跑 gen:types 会产生 diff。 | 整合后统一跑 gen:types 并 commit |
| **订单状态硬编码** | 中 | dashboard.service.ts 硬编码 7 个订单状态字符串，C 流程 OrderStatus enum 若不同会静默出错。 | 整合时对比 C 流程的 OrderStatus 定义，改为从 shared-types 引入 |
| **app.module.ts 合并** | 低 | M 加了 PlatformModule，字母序在 AuthModule 和 RealtimeModule 之间。W/C 也会加各自的 Module。 | 字母序合并，无冲突 |
| **schema.prisma 合并** | 低 | M 加了 3 个 model（SystemConfig/Settlement/WithdrawalRequest），末尾追加。无 enum 冲突。 | W→C→M 顺序追加 |
| **migration 时间戳** | 低 | `20260623120000_add_platform_settle_m` — _m 后缀字母序最后，不会与 W/C 撞。 | 直接复制 |
| **errors.json 合并** | 低 | M 加了 E-PLATFORM-* / E-AUDIT-*，在 §3.4 M 流程范围内。不会与 W/C 撞。 | union 合并 |
| **shared-locales 合并** | 低 | M 加了 platform.json bundle。W/C 不碰 platform namespace。 | 字母序合并 |
| **seed.ts 合并** | 低 | M 加了 `// === FLOW M ===` 段。W/C 各自有自己的段。 | 按 FLOW 注释分段合并 |

---

## 五、整合就绪度评分

| 维度 | M | 说明 |
|---|---|---|
| 文件边界遵守 | 7/10 | (warehouse) 占位页违规扣 3 分 |
| 命名规范遵守 | 10/10 | 全部合规 |
| 代码质量 | 8/10 | gen:types 过期扣 1 分，dead export 扣 1 分 |
| Manifest 完整度 | 0/10 | 无 manifest |
| 跨流程依赖标记 | 7/10 | 订单状态硬编码无契约标记扣 3 分 |
| **综合** | **32/50** | **需修复 P0/P1 后整合** |

> ≥40 可直接整合；30-39 需修复 P0/P1 后整合；<30 建议返工。

---

## 六、完成度

### W2 任务（平台看板 + 视角切换）

| 任务 | 状态 | 备注 |
|---|---|---|
| M1 C1: GMV 统计(今日/本周/本月) | ✅ | dashboard.service.ts aggregateGmvAndOrders |
| M1 C1: 订单数趋势图 | ✅ | aggregateTrend (raw SQL + 补空桶) |
| M1 C1: 在线骑手实时数 | ✅ | countOnlineRiders (60s TTL) |
| M1 C1: 异常订单监控 | ✅ | countAbnormalOrders (CANCELLED + DELIVERED_UNPAID + 超时) |
| M1 C1: 仓库维度数据钻取 | ✅ | aggregateWarehouseBreakdown (GMV + 异常 by warehouse) |
| M1 C2: 菜单按视角动态渲染 | ❌ | 只有 PerspectiveSwitcher 下拉框，无侧边栏菜单 |
| M1 C2: 路由守卫按视角控制 | ✅ | PerspectiveGuard 组件 |
| M1 C2: 数据请求自动带 perspective | ✅ | fetch.ts apiFetch + X-Perspective header |
| M1 C2: 视角切换动画与提示 | ✅ | PerspectiveSwitcher toast |
| M1 C2: 各视角首页落地 | ✅ | 5 个占位页（含 (warehouse) 违规） |

**W2 完成度：9/10 (90%)**

### W4 任务（审计 + 系统配置，提前到 W2）

| 任务 | 状态 | 备注 |
|---|---|---|
| M1 C1: 审计日志列表 | ✅ | cursor 分页 + 筛选 |
| M1 C1: 审计详情 | ✅ | 含 before/after 快照 |
| M1 C1: 审计导出 CSV | ✅ | 10000 行上限 + CSV escape |
| M1 C2: 系统配置 CRUD | ✅ | list + update (无 create/delete，key 由 seed 预置) |
| M1 C2: 配置变更审计 | ✅ | @Audit 装饰器 |
| M1 C2: 配置缓存 Redis | ✅ | cache-aside (get→miss→db→set, update→del) |

**W4 完成度：6/6 (100%，提前完成)**

### W3 任务（IM + 结算）

- Schema 预建：Settlement + WithdrawalRequest model ✅
- IM 模块：未创建（W3 范围）
- Settle 模块：未创建（W3 范围）

**W3 完成度：0/13 (0%，预期)**

---

## 七、建议下一步

1. **[P0] 提交所有改动** — 按 task 粒度分 commit，格式 `[W2-M-C{n}-T{n}] 简述`
2. **[P1] 生成 W2-M-MANIFEST.md** — 按 §4.1 模板，列出独占文件 + 共享文件改动 + 自检结果
3. **[P1] 重新跑 gen:types 并提交** — `pnpm --filter @meimart/shared-types gen:types`，确认 api-types.ts 更新
4. **[P1] 删除 (warehouse) 占位页** — 或移到 M 流程自己的路由组下
5. **[P1] 删除 dashboard.controller.ts 的 dead export** — 移除 `import { Public }` 和 `_platformDashboardPublic`
6. **[P1] 修复 dashboard 时区** — `AT TIME ZONE 'UTC'` → `AT TIME ZONE 'Asia/Dili'`
7. **[P1] 修复 audit controller 错误码** — `E-COMMON-001` → `E-AUDIT-002`
8. **[P1] 加跨流程契约标记** — dashboard.service.ts 订单状态字符串加 TODO 注释
