# MeiMart W2 流程 C 审查报告

- **日期**：2026-06-24
- **审查者**：Hermes Agent（流程 C 审核员）
- **范围**：W2 流程 C（交易/配送）— `w2-flow-c` 分支
- **结论**：⚠️ 修复后可整合（0 P0 + 5 P1 + 6 P2）

---

## 一、全局概览

| 流程 | 目录 | 分支 | commit 数 | typecheck | test | manifest |
|---|---|---|---|---|---|---|
| C | MeiMart-c | w2-flow-c | 4 | ⚠️ 1 | ✅ 46 | ✅ |

> typecheck：apps/api 报 `tsconfig.json ignoreDeprecations` 错误，属 W1 遗留问题（78abed4 commit 已说明），CI 能过。其余 6 个 workspace 全过。
> test：46 passed，W1 留下的 5 个 spec 文件无 regression。新增模块（order/payment/cart）无单测。
> manifest：W2-C-MANIFEST.md 已输出，结构完整，符合 §4.1 模板。

### Commit 历史

```
e8450eb [W2-C-D7] W2-C-MANIFEST.md 输出（流程 C W2 阶段完成报告）
fa0b321 [W2-C-D4] admin-web 商家/仓库订单视角页面骨架
22533ac [W2-C-D2] 类型修复 + i18n 5 语言同步 + openapi.yaml 重新生成
73ee80e Merge branch 'main' of github.com:LSW-U/MeiTest into w2-flow-c
0d9fe74 [W2-C-D1] order/payment/cart 三模块骨架 + 契约 5 schema
78abed4 [W2-prepare] 前端 repo 边界（MeiMart1.0 独立 + 本 repo 仅后端+admin-web）
```

---

## 二、问题清单

### P0 — 阻断整合（必须修复）

无。

### P1 — 强烈建议修复

| # | 问题 | 文件 | 修复建议 |
|---|---|---|---|
| P1-1 | **文件边界违规：(warehouse) 路由组** — C 流程创建了 `apps/admin-web/src/app/(warehouse)/orders/page.tsx`，但 §2.1 明确将 `(warehouse)/**` 划归 W 流程独占。整合时与 W 流程的 (warehouse) 页面会冲突。 | apps/admin-web/src/app/(warehouse)/orders/page.tsx | 删除此文件，或改到 C 流程自己的路由组下（如 `(merchant)/orders/`，§2.2 未将 (merchant) 分配给任何流程，属灰色地带但至少不与 W 撞）。整合时由 W 流程填充 (warehouse) 内容。 |
| P1-2 | **改了 W1 文件 shared/db/index.ts** — 新增 `releaseStock` 和 `StockChangeContext` 的 export。虽然 manifest §2 已报备且理由合理（W1 transaction.ts 已实现 releaseStock 但 index.ts 漏 export），但 §2.4 规定 `shared/**` 不改。 | apps/api/src/shared/db/index.ts | 整合时主 AI 确认 transaction.ts 确实已有 releaseStock 函数（而非 C 流程新增），则接受此 export 扩展。如 C 流程在 transaction.ts 里加了代码则需审查。 |
| P1-3 | **新增模块零单测** — order（635 行 service）、payment（321 行 service）、cart（260 行 service）、order-status.machine（纯函数状态机）、order-no.service（Redis INCR）均无单测。CLAUDE.md §测试要求"关键逻辑必须单测，覆盖率 ≥ 70%"，鉴权/支付/库存必须有 e2e。 | apps/api/src/modules/order/、payment/、cart/ | 至少补 order-status.machine.ts（纯函数，最易测）+ order-no.service.ts（mock Redis）+ payment.service.mockCallback 的单测。W3 联调前必须补。 |
| P1-4 | **admin-web 页面硬编码英文 + 内联样式** — merchant/orders 和 warehouse/orders 页面大量硬编码英文（"Accept"、"Mark Picked"、"Warehouse Picking"、"Order No" 等），违反 CLAUDE.md §代码风格"严禁硬编码字符串，所有 UI 文案用 i18n key"。同时全部用内联 style 对象 + 硬编码颜色（#1a5dc2），未用 shadcn/ui + theme token。 | apps/admin-web/src/app/(merchant)/orders/page.tsx、apps/admin-web/src/app/(warehouse)/orders/page.tsx | 改用 next-intl t() + shadcn/ui 组件 + theme token。即使 W2 骨架阶段也应遵守规范。 |
| P1-5 | **admin-web 代码异味：void 抑制未使用变量** — merchant/orders/page.tsx L76-78 有 `void apiFetch; void setOrders; void filter;` 在 useEffect 中抑制 unused 警告。骑手端 CLAUDE.md §30 明确禁止 `void Xxx;` 写法。 | apps/admin-web/src/app/(merchant)/orders/page.tsx L76-78 | 删掉整个空 useEffect（它什么都不做），或改为真正的 API 调用。W2 骨架阶段可直接删。 |

### P2 — 可推迟到整合后

| # | 问题 | 文件 | 修复建议 |
|---|---|---|---|
| P2-1 | **dispatch/rider/location/notify/refund 模块为空目录** — W3-W5 范围，可接受。Schema 已预建（RiderProfile/DeliveryTask/CashCollection 等 W1 init migration 已有）。 | apps/api/src/modules/{dispatch,rider,location,notify,refund}/ | W3-W5 实现时填充 |
| P2-2 | **schema.prisma 无改动、无新 migration** — W1 init migration 已含 C 流程所需全部表（29 张）。refund 表待 W5 实做时新增 migration `add_refund_c`。manifest 已说明。 | — | W5 补 |
| P2-3 | **ESLint 未跑** — manifest 自检 §5 标注"未跑"。apps/api 用 tsx esbuild 无 lint step。 | — | W3 前补 ESLint 配置 + 跑过 |
| P2-4 | **e2e 测试未跑** — 需 docker compose 起 postgres+postgis+redis，留 W3 集成测试阶段。 | — | W3 补 |
| P2-5 | **admin-web (merchant) 路由组归属未定义** — §2.1/§2.2/§2.3 均未将 `(merchant)/**` 分配给任何流程。C 流程创建了 `(merchant)/orders/page.tsx`，不在其明确独占范围内，但也不与任何流程冲突。 | apps/admin-web/src/app/(merchant)/ | 整合时由主 AI 决策归属 |
| P2-6 | **Tetum 翻译为英文** — cart.json tet 版本内容与 en 相同。与 W1 决策一致（"Tetum 留接口空翻译"）。 | packages/shared-locales/tet/cart.json | 后期补 Tetum 翻译 |

---

## 三、逐维度审查详情

### 维度 1：文件边界

**后端模块**：✅ 只创建了 `apps/api/src/modules/{order,cart,payment}/**`，未碰 W/M 独占后端文件（warehouse/catalog/inventory/pricing/shop/platform/settle/im/audit）。

**W1 文件**：⚠️ 改了 `apps/api/src/shared/db/index.ts`（新增 releaseStock + StockChangeContext export）。manifest §2 已报备，理由是 W1 transaction.ts 已实现 releaseStock 但 index.ts 漏 export。属必要修复，未改 W1 业务逻辑。

**共享文件**：✅ 正确修改了 app.module.ts / api-contract/index.ts / gen-openapi.ts / shared-locales/index.ts / errors.json ×5 / openapi.yaml / api-types.ts。

**前端**：❌ 创建了 `(warehouse)/orders/page.tsx` — 违反 §2.1（W 流程独占）。`(merchant)/orders/page.tsx` 属灰色地带（未分配给任何流程）。

**client-app/rider-app 文件删除**：diff 中包含 `apps/client-app/*` 和 `apps/rider-app/*` 的删除——这是 merge 78abed4（前端 repo 迁出）的结果，非 C 流程主动改动，无违规。

### 维度 2：命名规范

- Prisma model：✅ 无新增 model（W1 已建全），无需检查
- Migration：✅ 无新增 migration（W1 init 已含全部表），无需检查
- Schema export：✅ `cart.ts` / `dispatch.ts` / `payment.ts` / `refund.ts` / `rider.ts` — 业务名 kebab-case，符合 §3.3
- Schema 变量命名：✅ `Cart` / `CartItem` / `AddCartItemRequest` / `PaymentIntent` / `CheckoutPreview` 等 — PascalCase 业务名，符合 §3.3
- 错误码：✅ E-ORDER-005 / E-CART-001~004 / E-PAYMENT-004~010 / E-DISPATCH-001~005 / E-RIDER-001~005 — 全在 §3.4 C 流程范围内
- i18n key：✅ cart.json 独占 namespace，未碰 common.json
- Seed 分段：✅ 无改动（未加 seed）

### 维度 3：共享文件改动记录

| 共享文件 | 改动 | manifest 记录 |
|---|---|---|
| app.module.ts | + CartModule / OrderModule / PaymentModule import + imports 数组（字母序） | ✅ |
| schema.prisma | 无改动 | ✅ 已说明 |
| migrations/ | 无新增 | ✅ 已说明 |
| api-contract/index.ts | + 5 个 export（cart/payment/dispatch/rider/refund） | ✅ |
| gen-openapi.ts | + 8 个 schema 注册 + 14 条 path 注册 | ✅ |
| shared-locales/index.ts | + cart bundle 5 语言 import + 注册 | ✅ |
| common.json ×5 | 无改动 | ✅ 已说明 |
| errors.json ×5 | + E-ORDER-005 / E-CART-001~004 / E-PAYMENT-004~010 / E-DISPATCH-001~005 / E-RIDER-001~005 | ✅ |
| seed.ts | 无改动 | ✅ 已说明 |
| openapi.yaml | 重新生成（paths 14→20, schemas 26→34） | ✅ 产物已 commit |
| api-types.ts | 重新生成 | ✅ 产物已 commit |
| shared/db/index.ts | + releaseStock + StockChangeContext export | ✅ 已报备 |

### 维度 4：代码质量

- **typecheck**：⚠️ apps/api 报 `tsconfig.json(3,3): error TS5103: Invalid value for '--ignoreDeprecations'` — W1 遗留问题，78abed4 已说明，CI 能过。其余 6 个 workspace 全过。
- **test**：✅ 46 passed（W1 基线无 regression）。❌ 新增模块零单测。
- **gen:openapi**：✅ 产物与工作区一致（paths 20, schemas 34），`git diff --exit-code` 无变更。
- **gen:types**：✅ 产物与工作区一致，`git diff --exit-code` 无变更。
- **ESLint**：❌ 未跑。

### 维度 5：跨流程依赖标记

**W 流程的对外接口（C 会调用）**：

- `findWarehouseByPoint(db, lng, lat)` — C 流程 order.service.ts 下单时调用，**直接用 W1 shared/db 的 PostGIS helper，非 mock**。函数签名 `findWarehouseByPoint(db, lng, lat) → { id, code, deliveryFee, ... } | null`。✅ 无需 mock，W1 已实现。
- 商品/库存数据 — C 流程 order.service.ts 查 `db.sku.findUnique` + `db.product`，cart.service.ts 查 `db.sku`。这些是 W1 init migration 已建的表，**非 W 流程新增**。✅ 无需 mock。
- `matchWarehouse(lat, lng) → warehouseId` — C 流程没有单独的 matchWarehouse 函数，直接用 findWarehouseByPoint 返回完整 warehouse 对象。✅ 接口对齐无风险。
- perspective store — C 流程不涉及（那是 M 流程的 admin-web 基建）。

**C 流程的 mock 点**：

- PaymentStrategy — W1 已封装 5 策略（COD/BANK 真实 + WECHAT/PAYPAL/STRIPE mock/stub），C 流程 payment.service.ts 直接调用 `getPaymentStrategy()` 获取策略实例。mockFlag 字段贯穿 PaymentIntent 全生命周期。✅ 标记清晰。
- mockCallback — payment.service.ts L154-210，dev/staging 模拟第三方支付成功。有 NODE_ENV !== 'production' 守卫 + logger.warn 标记。✅ 标记清晰。
- admin-web 页面 — merchant/orders 和 warehouse/orders 用 MOCK_ORDERS / MOCK_TASKS 硬编码数据。代码注释标了 "W3 联调时接真 API"。✅ 标记清晰。

**跨流程依赖契约标记**：

- order.service.ts 硬编码了订单状态字符串（'PENDING_CONFIRM' / 'CONFIRMED' / 'CANCELLED' 等），但这些来自 Prisma enum（OrderStatusValue），不是 C 流程自造的。W1 schema.prisma 已定义 OrderStatus enum，M 流程 dashboard 若也引用同一 enum 则无风险。✅ 无隐式契约依赖。

### 维度 6：安全检查

- **@Roles 标注**：✅ 三个新 controller 全部标注
  - OrderController: `@Roles('customer')` @ `api/v1/client/orders`
  - PaymentController: `@Roles('customer')` @ `api/v1/client/payments`
  - CartController: `@Roles('customer')` @ `api/v1/client/cart`
- **APP_GUARD 全局鉴权**：✅ 依赖 W1 全局 JwtAuthGuard，无需手动 @UseGuards
- **硬编码密钥**：✅ 无（realtime.gateway.ts 中的 'Bearer xxx' 是注释示例，非实际密钥）
- **SQL 注入**：✅ 新增代码无 `$queryRaw` / `$executeRaw`（health.controller 的 `SELECT 1` 是 W1 代码）
- **Logger mask**：✅ 使用 W1 的 logger（ALS traceId），新代码中未记录 phone/email/idCard 等敏感字段
- **CORS / JWT secret**：✅ 未改动
- **NODE_ENV 守卫**：✅ mockCallback 有 `process.env.NODE_ENV === 'production'` 检查

### 维度 7：W1 已完成项重复检查

- ✅ 未重复 auth/RBAC/JWT/device_type
- ✅ 未重复 OTP 策略
- ✅ 未重复 PaymentStrategy（直接复用 W1 infrastructure/payment 的 5 策略）
- ✅ 未重复 i18n 基础（只新增 cart namespace）
- ✅ 未重复 docker compose
- ✅ 未重复 RealtimeGateway（未碰 realtime 模块）
- ✅ 未重复 GIST 索引
- ✅ 未重复 init migration

---

## 四、整合风险预判

| 风险点 | 严重度 | 详情 | 整合时处理方案 |
|---|---|---|---|
| **(warehouse) 路由组冲突** | 高 | C 流程创建了 `(warehouse)/orders/page.tsx`，W 流程 §2.1 也独占 `(warehouse)/**`。整合时 git merge 会冲突。 | 删除 C 流程的 (warehouse) 占位页，由 W 流程创建 |
| **shared/db/index.ts 合并** | 中 | C 流程扩展了 export（+releaseStock +StockChangeContext）。W/M 流程也可能改此文件。 | 检查 W/M 是否也改了 index.ts，union 合并 export |
| **app.module.ts 合并** | 低 | C 加了 CartModule / OrderModule / PaymentModule，字母序在 AuthModule 和 RealtimeModule 之间。W/M 也会加各自的 Module。 | 字母序合并，无冲突 |
| **api-contract/index.ts 合并** | 低 | C 加了 5 个 export（cart/payment/dispatch/rider/refund），字母序。 | 字母序合并 |
| **shared-locales/index.ts 合并** | 低 | C 加了 cart bundle 5 语言。W/M 各自有自己的 bundle。 | 字母序合并 |
| **errors.json 合并** | 低 | C 加了 E-ORDER/CART/PAYMENT/DISPATCH/RIDER 段，在 §3.4 C 流程范围内。不会与 W/M 撞。 | union 合并 |
| **gen-openapi.ts 合并** | 中 | C 加了 8 个 schema 注册 + 14 条 path 注册。W/M 也会加各自的注册。需确认 import 和注册无重复。 | 按 schema 名去重合并 |
| **findWarehouseByPoint 接口对齐** | 无风险 | C 直接用 W1 shared/db 的函数，非 mock，签名固定。 | 无需处理 |

---

## 五、整合就绪度评分

| 维度 | C | 说明 |
|---|---|---|
| 文件边界遵守 | 7/10 | (warehouse) 占位页违规扣 2 分，shared/db/index.ts 改动扣 1 分（已报备） |
| 命名规范遵守 | 10/10 | 全部合规 |
| 代码质量 | 7/10 | typecheck W1 遗留扣 1 分，零单测扣 1 分，ESLint 未跑扣 1 分 |
| Manifest 完整度 | 9/10 | 结构完整，自检诚实（标了未跑项），shared/db 报备清晰 |
| 跨流程依赖标记 | 9/10 | mock 点标记清晰，无隐式契约依赖。findWarehouseByPoint 直接用 W1 函数无 mock 需求。扣 1 分因 admin-web mock 数据无 TODO 标记 |
| **综合** | **42/50** | **≥40 可直接整合，但建议先修 P1-1（删 (warehouse) 页面）** |

> ≥40 可直接整合；30-39 需修复 P0/P1 后整合；<30 建议返工。

---

## 六、完成度

### W2 任务（订单核心 + 支付 + 购物车）

| 任务 | 状态 | 备注 |
|---|---|---|
| M1 order C1: 订单状态机（10 状态 + 流转规则） | ✅ | order-status.machine.ts，纯函数，设计清晰 |
| M1 order C2: 下单核心（PostGIS 匹配仓库 + 同步事务 + 行锁防超卖） | ✅ | order.service.ts createOrder，635 行 |
| M1 order C2: orderNo 生成（Redis INCR + Asia/Dili 时区） | ✅ | order-no.service.ts，16 位格式，2 天 TTL |
| M1 order C2: 取消订单（释放库存 + 状态机 + OrderEvent） | ✅ | order.service.ts cancelOrder |
| M1 order C2: 支付成功回调（PENDING_PAYMENT → CONFIRMED） | ✅ | order.service.ts markPaid |
| M1 payment C1: PaymentIntent 创建 + 状态流转 | ✅ | payment.service.ts createIntentForOrder |
| M1 payment C1: mock 回调（dev/staging） | ✅ | payment.service.ts mockCallback，有 prod 守卫 |
| M1 payment C1: 银行转账凭证上传 | ✅ | payment.service.ts uploadReceipt |
| M1 cart C1: 购物车 CRUD（加购/改量/选中/删除） | ✅ | cart.service.ts，DB 持久化 |
| M1 cart C1: 结算前预览（库存校验 + 仓库匹配 + 运费） | ✅ | cart.service.ts previewCheckout |
| M1 cart C1: 下单后清空已购 items | ✅ | cart.service.ts clearOrderedItems |
| M1 admin-web: 商家订单视角页面骨架 | ✅ | (merchant)/orders/page.tsx，mock 数据 |
| M1 admin-web: 仓库拣货视角页面骨架 | ✅ | (warehouse)/orders/page.tsx，mock 数据（但违规） |

**W2 完成度：13/13 (100%)**

### W3 任务（配送 + 骑手）

- dispatch/rider/location/notify 模块：空目录，未实现
- 契约 schema：dispatch.ts / rider.ts 已预建

**W3 完成度：0%（预期）**

### W5 任务（退款）

- refund 模块：空目录
- 契约 schema：refund.ts 已预建

**W5 完成度：0%（预期）**

---

## 七、建议下一步

1. **[P1] 删除 (warehouse)/orders/page.tsx** — 或移到 (merchant) 路由组下，避免与 W 流程冲突
2. **[P1] 补 order-status.machine.ts 单测** — 纯函数最易测，canTransition / assertCanTransition / getInitialState / isUserCancellable 覆盖全部状态流转
3. **[P1] 修复 admin-web 代码异味** — 删 `void apiFetch; void setOrders; void filter;`，改用 i18n key + shadcn/ui 组件
4. **[P1] 确认 shared/db/transaction.ts 中 releaseStock 是否为 W1 已有** — 如是则接受 index.ts export 扩展；如 C 流程新增了 transaction.ts 代码则需审查
5. **[P2] W3 前补 payment.service.mockCallback + order-no.service 单测** — 支付 + orderNo 是关键逻辑
6. **[P2] W3 前跑 ESLint** — 至少对新增文件跑一次

---

## 八、亮点

- **订单状态机设计优秀** — 纯函数、无 DB 调用、10 状态 + 流转规则清晰、入口分叉按 PaymentMethod 自动决定初始态
- **下单流程完整** — PostGIS 匹配仓库 → 同步事务 → 行锁防超卖 → orderNo Redis INCR → PaymentIntent 创建 → 失败自动取消，635 行 service 覆盖全链路
- **mock/stub 标记规范** — PaymentIntent.mockFlag 贯穿全生命周期，mockCallback 有 NODE_ENV 守卫 + logger.warn
- **直接复用 W1 基建** — findWarehouseByPoint / deductStock / releaseStock / getPaymentStrategy / withTransaction 全部复用 W1，无重复造轮子
- **app.module.ts 字母序插入** — 主动加了注释说明字母序排列原因，便于三流程 merge
- **manifest 自检诚实** — 明确标注未跑项（ESLint、e2e、新模块单测），不隐瞒
- **gen:openapi + gen:types 产物已 commit** — 整合时无需重新生成
- **错误码全在 C 流程段** — E-ORDER/CART/PAYMENT/DISPATCH/RIDER，不与其他流程撞
