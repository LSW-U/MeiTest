# MeiMart W2 流程 W 审查报告

- **日期**：2026-06-24
- **审查者**：Hermes Agent（流程 W 审核员）
- **范围**：W2 流程 W（供给/仓储/浏览）— `w2-flow-w` 分支
- **结论**：✅ 可整合（综合评分 40/50，达标）

---

## 一、全局概览

| 流程 | 目录 | 分支 | commit 数 | typecheck | test | manifest |
|---|---|---|---|---|---|---|
| W | MeiMart-w | w2-flow-w | 11 | ⚠️ 6/7 过 | ✅ 122 passed | ✅ 有 |

> apps/api typecheck 失败是 W1 已知的 TS 6.0.3 `ignoreDeprecations` 问题，非 W 流程引入，不影响实际代码。

---

## 二、问题清单

### P0 — 阻断整合（必须修复）

无。

### P1 — 强烈建议修复

| # | 问题 | 文件 | 修复建议 |
|---|---|---|---|
| P1-1 | **common.json i18n key 未按 §3.6 命名** — `perspective.title` 应为 `w.perspective.title`（§3.6 要求共用 namespace 按 `{flow}.{feature}.{key}` 命名）。manifest §4.3 已报备但不合规。 | packages/shared-locales/*/common.json | 改 key 为 `w.perspective.title` 等，或如果 M 流程也用 perspective 则提取为 `common.perspective.title`（跨流程共用 key，manifest 报备） |
| P1-2 | **admin-web 路由组未用括号** — §2.1 W 流程独占 `apps/admin-web/src/app/(shop)/**` 和 `(warehouse)/**`，但实际创建的是 `app/shop/`、`app/warehouse/`、`app/catalog/`（无括号）。Next.js App Router 中 `(group)` 不影响 URL 但影响路由分组隔离。 | apps/admin-web/src/app/shop/、warehouse/、catalog/ | 整合时改为 `(shop)/`、`(warehouse)/`、`(catalog)/` 括号路由组 |
| P1-3 | **seed.ts 分段注释不符合 §3.5** — §3.5 要求 `// === FLOW W ===` 格式，实际用的是 `// ===== 7. W 流程扩展 =====`。 | apps/api/prisma/seed.ts L220 | 改为 `// === FLOW W ===` 标准格式，方便主 AI 按注释定位段落 |
| P1-4 | **缺少模块专属错误码** — catalog/inventory/pricing/shop 四个模块均无专属错误码（E-CATALOG-* / E-INVENTORY-* / E-PRICING-* / E-SHOP-*）。当前错误处理用的是 E-COMMON-001/003 通用码。§3.4 为这些模块预留了 001-099 段。 | packages/shared-locales/*/errors.json | 补充各模块关键错误的专属错误码（如 E-INVENTORY-001 库存不足、E-CATALOG-001 商品不存在等） |

### P2 — 可推迟到整合后

| # | 问题 | 文件 | 修复建议 |
|---|---|---|---|
| P2-1 | **PostGIS 集成测试用 vi.mock 跳过** — findWarehouseByPoint/setWarehouseGeometry 单测 mock 了 raw SQL，未覆盖真实 PostGIS 行为。manifest §6.1 已标记推 W6 补。 | apps/api/tests/warehouse.service.test.ts | W6 用 testcontainers 起真实容器补 |
| P2-2 | **pricing 用默认值 0** — perKmFee / minOrderAmount 硬编码为 0，manifest §6.2 已标记推 M 流程扩展。 | apps/api/src/modules/pricing/pricing.service.ts | M 流程做 system_config 时扩展 |
| P2-3 | **admin-web 未做浏览器实测** — 3 个页面已写但未在真实 backend 环境验证。manifest §6.3 已标记。 | apps/admin-web/src/app/* | 整合后跑 docker compose + pnpm dev 实测 |
| P2-4 | **Coupon 接口未实现** — 前端 MeiMart1.0 调 `/user/coupons` 但无 Coupon model。manifest §6.4 已标记推 M 流程。 | — | M 流程 platform 模块补 |
| P2-5 | **catalog 路由组不在 §2.1 独占列表** — `(catalog)/**` 未在 §2.1 明确列为 W 流程独占，但 W-M-C-T 任务分解 W4 包含"商品管理 admin-web"。属规范灰色地带。 | — | 整合时由主 AI 确认归属 |

---

## 三、逐维度审查详情

### 维度 1：文件边界

**C/M 独占文件**：✅ 无违规。未碰 order/cart/payment/refund/dispatch/rider/location/notify/platform/settle/im/audit。

**W1 完成文件**：✅ 已报备。auth/** 被修改（auth.controller.ts 新建 + auth.service.ts 扩展 + auth.module.ts 扩展），manifest §4.1 详细报备，理由充分（W1 只有 mock-login，需补生产端点）。mock-login.controller.ts 未动，prod 条件注册保留。health/me/shared/infrastructure 未碰。

**client-app/rider-app 删除**：✅ 非违规。这是 78abed4 main 同步带来的前端 repo 迁出（§2.5），不是 W 流程的改动。

### 维度 2：命名规范

- **Prisma model**：✅ 无新 model（复用 W1 的 29 张表）
- **Migration**：✅ 无新 migration（未改 schema）
- **Schema export**：✅ catalog.ts 新建，变量用 `XxxRequest` / `XxxResponseData` 命名
- **错误码**：⚠️ E-AUTH-011~015（扩 W1 段，已报备）、E-WAREHOUSE-001~002（W 段）。但 catalog/inventory/pricing/shop 模块缺专属错误码（P1-4）
- **i18n key**：⚠️ `perspective.*` 未带 flow 前缀（P1-1），manifest 已报备但不合规
- **seed 分段**：⚠️ 用 `// ===== 7. W 流程扩展` 而非 `// === FLOW W ===`（P1-3）

### 维度 3：共享文件改动记录

| 共享文件 | 改动 | manifest 记录 |
|---|---|---|
| app.module.ts | +6 Module import | ✅ §2 记录 |
| schema.prisma | 无改动（复用 W1） | ✅ §2 记录 |
| migrations/ | 无新增 | ✅ §2 记录 |
| contract index.ts | +`export * from './catalog'` | ✅ §2 记录 |
| gen-openapi.ts | +~30 schema register + ~25 paths + 6 tags | ✅ §2 记录 |
| shared-locales/index.ts | 无改动 | ✅ §2 记录 |
| seed.ts | +地址/收藏/通知/分类/Banner | ✅ §2 记录 |
| errors.json ×5 | +E-AUTH-011~015 + E-WAREHOUSE-001~002 | ✅ §2 记录 |
| common.json ×5 | +perspective.* 6 个 key | ✅ §4.3 记录 |
| openapi.yaml | 重新生成（44 paths, 51 schemas） | ✅ 产物已 commit |
| api-types.ts | 重新生成 | ✅ 产物已 commit |

### 维度 4：代码质量

- **typecheck**：⚠️ 6/7 workspace 过。apps/api TS 6.0.3 deprecation（W1 已知遗留）
- **test**：✅ 122 passed（11 test files，含 auth 28 + catalog 14 + user 14 + warehouse 10 + inventory 8 + pricing 7 + shop 5 + roles 5 + realtime 13 + 其他）
- **gen:openapi**：✅ 44 paths, 51 schemas，git diff --exit-code 无变更
- **gen:types**：✅ git diff --exit-code 无变更

### 维度 5：跨流程依赖标记

**W 流程对外接口（C/M 会调用）**：
- `matchWarehouse(lat, lng) → warehouseId` — ✅ 实现在 inventory.service.ts L39，通过 HTTP endpoint `POST /api/v1/client/inventory/match-warehouse` 暴露，C 流程下单时调用
- `calcDeliveryFee(warehouseId, lat, lng) → fee` — ✅ 实现在 pricing.service.ts，通过 HTTP endpoint `GET /api/v1/client/pricing/delivery-fee` 暴露
- 商品/库存查询接口 — ✅ `GET /api/v1/client/products`、`GET /api/v1/client/inventory/:skuId`

**mock/stub 标记**：
- SMS stub：✅ 固定 123456，标 `[SMS_STUB]`，注释 "W6 切东帝汶本地"
- PostGIS：⚠️ 单测用 vi.mock 跳过真实 SQL（P2-1，已标记 W6 补）
- pricing 默认值：⚠️ perKmFee=0, minOrderAmount=0（P2-2，已标记 M 流程扩展）

### 维度 6：安全检查

- **@Roles 标注**：✅ 所有新 controller 均标注
  - user.controller.ts：@Roles('customer')
  - shop.controller.ts：GET @Public + PATCH @Roles('super_admin')
  - warehouse.controller.ts：@Roles('super_admin', 'warehouse_staff')
  - catalog.controller.ts：list/detail @Public + 管理接口 @Roles('super_admin', 'warehouse_staff')
  - inventory.controller.ts：match @Roles('customer') + 管理接口 @Roles('super_admin', 'warehouse_staff')
  - pricing.controller.ts：calc @Roles('customer') + config @Roles('super_admin')
- **APP_GUARD 全局鉴权**：✅ 依赖 W1 全局 JwtAuthGuard，无需手动 @UseGuards
- **硬编码密钥**：✅ 无（"Bearer xxx" 仅为注释示例）
- **SQL 注入**：✅ warehouse.service.ts L169 用 `$queryRaw` tagged template，参数自动参数化
- **CORS / JWT secret**：✅ 未改动
- **mock-login 安全**：✅ prod 环境不注册（auth.module.ts L28 条件判断保留）

### 维度 7：W1 已完成项重复检查

- ✅ 未重复 auth/RBAC/JWT/device_type（扩展而非重做）
- ✅ 未重复 OTP 策略（W1 的 PasswordStrategy/SmsStrategy 保持，W 流程补了业务层调用）
- ✅ 未重复 PaymentStrategy
- ✅ 未重复 i18n 基础（只新增 perspective key）
- ✅ 未重复 docker compose
- ✅ 未重复 RealtimeGateway
- ✅ 未重复 GIST 索引（复用 W1 init migration）
- ✅ 未重复 PostGIS helper（复用 shared/db/postgis-helpers.ts）

---

## 四、整合风险预判

| 风险点 | 严重度 | 详情 | 整合时处理方案 |
|---|---|---|---|
| **app.module.ts 合并** | 低 | W 加 6 Module（UserModule, ShopModule, WarehouseModule, CatalogModule, InventoryModule, PricingModule）。M 已加 PlatformModule。字母序排列无冲突。 | 字母序合并 |
| **schema.prisma 合并** | 无 | W 未改 schema | 无需处理 |
| **migration 合并** | 无 | W 未加 migration | 无需处理 |
| **contract index.ts 合并** | 低 | W 加 `export * from './catalog'`，M 加 `export * from './platform'` | 字母序合并 |
| **errors.json 合并** | 低 | W 加 E-AUTH-011~015 + E-WAREHOUSE-001~002，M 加 E-PLATFORM/E-AUDIT。不撞。 | union 合并 |
| **common.json 合并** | 中 | W 加 `perspective.*` key。M 流程如果也加了 perspective 相关 key 可能撞。 | 整合时对比，同 key 同值保留，不同值主 AI 决策 |
| **seed.ts 合并** | 低 | W 用 `// ===== 7. W 流程扩展` 注释，M 用 `// === FLOW M ===`。注释格式不统一但不影响执行。 | 整合时统一为 `// === FLOW W ===` 格式 |
| **matchWarehouse 接口对齐** | 低 | W 实现签名：`matchWarehouse(lat: number, lng: number) → warehouseId`。C 流程需调用此接口。 | 整合后验证 C 流程的调用签名与 W 一致 |
| **admin-web 路由组** | 低 | W 创建 `app/shop/`、`app/warehouse/`、`app/catalog/` 无括号。M 创建 `(platform)/`、`(merchant)/` 等有括号。 | 整合时给 W 的路由组加括号 |

---

## 五、整合就绪度评分

| 维度 | W | 说明 |
|---|---|---|
| 文件边界遵守 | 9/10 | auth 改动已报备，无 C/M 违规。扣 1 分：admin-web 路由组缺括号 |
| 命名规范遵守 | 7/10 | i18n key 无 flow 前缀扣 2 分，seed 注释格式扣 1 分 |
| 代码质量 | 8/10 | typecheck 有 W1 遗留问题扣 1 分，PostGIS 测试 mock 扣 1 分 |
| Manifest 完整度 | 9/10 | 非常详细，auth 报备充分，breaking change 列表完整。扣 1 分：seed 注释格式 |
| 跨流程依赖标记 | 7/10 | matchWarehouse 已实现并暴露，但缺专属错误码扣 2 分，pricing 默认值扣 1 分 |
| **综合** | **40/50** | **≥40 可直接整合** |

> ≥40 可直接整合；30-39 需修复 P0/P1 后整合；<30 建议返工。

---

## 六、完成度

### W2 任务（用户与商家基础）

| 任务 | 状态 | 备注 |
|---|---|---|
| M1 C1: 三端登录注册 | ✅ | auth.controller.ts 6 endpoint（password/SMS 登录、注册、SMS 码、密码重置、刷新） |
| M1 C2: RBAC + 视角切换基础架构 | ✅ | PerspectiveSwitcher + perspective.tsx Provider |
| M1 C3: 用户安全 | ✅ | device_type 隔离（W1 基建），登出（refresh token 撤销） |
| M2 C1: 用户资料 | ✅ | GET/PATCH /api/v1/client/user/profile |
| M2 C2: 收货地址 | ✅ | CRUD + 默认地址管理 + lat/lng |
| M3 C1: 商家信息 | ✅ | GET @Public + PATCH @Roles('super_admin') |
| M4 C1: i18n 基础 | ✅ | W1 已完成，W 扩展 perspective key |

**W2 完成度：7/7 (100%)**

### W3 任务（商品与仓储）

| 任务 | 状态 | 备注 |
|---|---|---|
| M1 C1: 仓库 CRUD | ✅ | warehouse module + PostGIS coverage 编辑 |
| M1 C2: 配送范围 | ✅ | PostGIS Polygon + GIST 索引（W1 建好，W 复用） |
| M1 C3: 营业时间与状态 | ✅ | schema 字段已有，API 暴露 |
| M2 C1: 商品 CRUD | ✅ | catalog module（列表/详情/创建/编辑/上下架） |
| M2 C2: SKU 与规格 | ✅ | SKU CRUD + 价格库存关联 |
| M2 C3: 分类管理 | ✅ | 分类树 + 筛选 + 排序 |
| M3 C1: 库存 | ✅ | inventory module + matchWarehouse + 批量调整 + 变更日志 |
| M3 C2: 防超卖 | ✅ | deductStock SELECT FOR UPDATE + 回库存 |
| M4 C1: 配送费 | ✅ | pricing module（基础费 + 每公里加价 + 距离计算） |
| M4 C2: 起送价 | ✅ | 起送价规则 + 校验 |

**W3 完成度：10/10 (100%)**

### W4 任务（客户端浏览 + 收尾）

| 任务 | 状态 | 备注 |
|---|---|---|
| admin-web 商家视角 | ✅ | app/shop/page.tsx |
| admin-web 仓库视角 | ✅ | app/warehouse/page.tsx |
| admin-web 商品视角 | ✅ | app/catalog/products/page.tsx |
| 客户端浏览页面 | ❌ | 归 MeiMart1.0 repo（§2.5 决策） |
| 流程 1 内部端到端 | 🟡 | API 层完整，浏览器实测未做 |

**W4 完成度：3/5 (60%，客户端页面按决策迁出)**

---

## 七、建议下一步

1. **[可直接整合]** W 流程综合评分 40/50 达标，P0 零项，可进入整合流程
2. **[P1 建议整合前修]** common.json i18n key 改为 `w.perspective.*` 或 `common.perspective.*`（带 flow 前缀或明确标记共用）
3. **[P1 建议整合前修]** admin-web 路由组加括号：`app/(shop)/`、`app/(warehouse)/`、`app/(catalog)/`
4. **[P1 建议整合前修]** seed.ts 注释改为 `// === FLOW W ===` 标准格式
5. **[P1 建议整合后补]** 补充 catalog/inventory/pricing/shop 模块专属错误码
6. **[P2 推 W6]** PostGIS 集成测试用 testcontainers 补真实容器测试
7. **[P2 推 M 流程]** pricing 的 perKmFee/minOrderAmount 从 system_config 读取
8. **[整合后]** 跑 docker compose + pnpm dev 做 admin-web 浏览器实测
