{
  "name": "@meimart/api-contract",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "gen:openapi": "tsx scripts/gen-openapi.ts",
    "mock": "echo 'TODO: prism mock (D6-T4)' && exit 0",
    "clean": "rm -rf dist .turbo openapi.yaml"
  },
  "dependencies": {
    "@asteasolutions/zod-to-openapi": "^7.3.4",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "yaml": "^2.9.0"
  }
}
