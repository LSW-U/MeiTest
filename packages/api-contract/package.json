{
  "name": "@meimart/api-contract",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "gen:openapi": "tsx scripts/gen-openapi.ts",
    "mock": "prism mock ./openapi.yaml --port 4010",
    "clean": "rm -rf dist .turbo openapi.yaml"
  },
  "dependencies": {
    "@asteasolutions/zod-to-openapi": "^7.3.4",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@stoplight/prism-cli": "^5.15.11",
    "tsx": "^4.22.4",
    "yaml": "^2.9.0"
  }
}
