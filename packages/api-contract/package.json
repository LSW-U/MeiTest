{
  "name": "@meimart/api-contract",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "gen:openapi": "echo 'TODO: tsx scripts/gen-openapi.ts (D1-T6)' && exit 0",
    "mock": "echo 'TODO: prism mock (D6-T4)' && exit 0",
    "clean": "rm -rf dist .turbo openapi.yaml"
  }
}
