{
  "name": "@meimart/shared-utils",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "echo 'TODO: vitest (D2-T2)' && exit 0",
    "test:coverage": "echo 'TODO: vitest --coverage (D2-T2)' && exit 0",
    "clean": "rm -rf dist .turbo coverage"
  }
}
