{
  "name": "@meimart/shared-types",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "gen:types": "echo 'TODO: openapi-typescript (D2-T1)' && exit 0",
    "clean": "rm -rf dist .turbo"
  }
}
