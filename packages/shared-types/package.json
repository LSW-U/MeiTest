{
  "name": "@meimart/shared-types",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "gen:types": "openapi-typescript ../api-contract/openapi.yaml -o src/api-types.ts",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "openapi-typescript": "^7.13.0"
  }
}
